apiVersion: v1
kind: Service
metadata:
  namespace: cicd-demo
  name: cicd-demo-service
spec:
  selector:
    app: cicd-demo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
